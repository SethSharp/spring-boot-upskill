spring:
  application:
    name: spring-boot-upskill
  datasource:
    url: jdbc:mysql://localhost:3306/spring_boot_upskill
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
